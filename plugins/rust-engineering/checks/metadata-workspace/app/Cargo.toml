[package]
name = "metadata-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
default = []
util = ["dep:renamed-util"]

[dependencies]
renamed-util = { workspace = true, optional = true }

[target.'cfg(unix)'.dependencies]
unix-util = { package = "metadata-local-util", path = "../metadata-local-util" }

