interface:
  display_name: "Rust Debugging"
  short_description: "Reproduce and isolate Rust failures"
  default_prompt: "Use $debugging to analyze the relevant Rust engineering decisions and return repository-grounded guidance."
policy:
  allow_implicit_invocation: true
