interface:
  display_name: "Nix Development Environments"
  short_description: "Build reproducible Rust dev shells"
  default_prompt: "Use $nix-dev-env to analyze the relevant Rust engineering decisions and return repository-grounded guidance."
policy:
  allow_implicit_invocation: true
