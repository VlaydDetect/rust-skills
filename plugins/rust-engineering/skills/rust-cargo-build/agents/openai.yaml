interface:
  display_name: "Cargo Build System"
  short_description: "Reason about Cargo builds and tooling"
  default_prompt: "Use $rust-cargo-build to resolve effective Cargo state, including reviewed scaffolding or worktree build tooling when explicitly relevant, and return repository-grounded guidance."
policy:
  allow_implicit_invocation: true
