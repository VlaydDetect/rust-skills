interface:
  display_name: "Rust Data Engineering"
  short_description: "Shape data around access patterns"
  default_prompt: "Use $rust-data to design this Rust data layout or execution path from its real access patterns."
policy:
  allow_implicit_invocation: true
