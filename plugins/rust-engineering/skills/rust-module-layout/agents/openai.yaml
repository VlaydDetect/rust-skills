interface:
  display_name: "Rust Module Layout"
  short_description: "Organize modules inside a crate"
  default_prompt: "Use $rust-module-layout to analyze the relevant Rust engineering decisions and return repository-grounded guidance."
policy:
  allow_implicit_invocation: true
