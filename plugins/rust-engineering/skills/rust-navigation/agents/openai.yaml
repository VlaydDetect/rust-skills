interface:
  display_name: "Rust Navigation"
  short_description: "Trace Rust symbols and call paths"
  default_prompt: "Use $rust-navigation to analyze the relevant Rust engineering decisions and return repository-grounded guidance."
policy:
  allow_implicit_invocation: true
