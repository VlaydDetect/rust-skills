# `docs` adapter

Product route:

- current std or crate documentation fact → `rust-research`;
- editing rustdoc, README, changelog, examples, or migration docs → `rust-documentation` through `rust-workflow`.

Match documentation to the exact toolchain/package version and resolved features. Do not copy `/latest/` text into a pinned-project dossier without marking the mismatch.
