# `rust-features` adapter

Product route: `rust-research rust <version-or-latest>`.

Return release date, language/compiler/library/Cargo/Clippy changes relevant to the question, stabilized APIs with minimum Rust versions, compatibility or migration notes, official links, retrieval date, confidence, and gaps. Compare with the project's MSRV/toolchain before recommending an API.
