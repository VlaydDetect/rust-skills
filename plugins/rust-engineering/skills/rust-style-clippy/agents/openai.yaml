interface:
  display_name: "Rust Style and Clippy"
  short_description: "Govern rustfmt and Clippy policy"
  default_prompt: "Use $rust-style-clippy to resolve lint levels, priorities, typed Clippy configuration, scope, and semantics-preserving remediation for the pinned toolchain."
policy:
  allow_implicit_invocation: true
