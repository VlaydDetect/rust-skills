interface:
  display_name: "Rust Testing"
  short_description: "Design Rust tests and coverage"
  default_prompt: "Use $rust-testing to design the test contract, loading reviewed nextest or LLVM coverage guidance only when the task explicitly requires it."
policy:
  allow_implicit_invocation: true
