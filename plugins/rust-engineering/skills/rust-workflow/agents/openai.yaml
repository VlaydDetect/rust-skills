interface:
  display_name: "Rust Workflow"
  short_description: "Route and implement Rust changes"
  default_prompt: "Use $rust-workflow to inspect the current Rust change slice, build a covered ProfileStack with one owner per decision unit, make the smallest coherent change, and verify the changed contract."
policy:
  allow_implicit_invocation: true
